# Rust
/target
**/target
**/*.rs.bk
*.profraw
/fuzz/artifacts
/fuzz/coverage
/fuzz/target

# Python
__pycache__/
*.py[cod]
.venv/
/dist
/wheels
*.so
*.pyd
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store

# Benchmark rival-result cache (generated; rivals are deterministic per config)
benchmarks/.bench_cache.json
benchmarks/.suite_cache.json
benchmarks/.fair_cache.json
benchmarks/.data_cache/
# Benchmark scratch (logs, probes, spikes — underscore-prefixed, regenerable)
benchmarks/_*
speed_accuracy_work.md
