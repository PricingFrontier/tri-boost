## BLOCKER: the area plans to be critiqued were not supplied

I was asked to act as PLAN CRITIC on "the assembled plan" / "the area plans below," but **no area plans were attached to my input, and none exist on disk.** I exhaustively searched: the repo (`/home/ralph/suite/tri-boost/` — only `spec/`, `design/`, `research/`, `AIM.md`, `.gitignore`; **zero code, zero plan files**), git history (3 commits, all spec/design, no plan), branches, stash, `/tmp`, env vars, and the parent `/home/ralph/suite/` tree. The only `*PLAN*` files in `suite/` belong to unrelated projects (`destyler`, `Linkedin`). The repo is at the pre-implementation stage: spec is final, nothing has been planned or built yet.

I cannot critique a plan that does not exist, and fabricating one to critique would mislead you. **Please re-run with the assembled area plans included in the prompt** (or written to a path I can read, e.g. `/home/ralph/suite/tri-boost/plans/`). Below is the spec-grounded critique rubric I will apply verbatim once they arrive — it doubles as a checklist the plan authors can self-test against.

## Critique rubric (spec-grounded, ready to apply)

**(1) ORDERING / DEPENDENCIES — nothing builds on an unverified foundation**
- The very first PR must land the gate machinery, not stubs-plus-gates-later. Required in PR1 (§1, §13.8): `#![forbid(unsafe_code)]`, `#![deny(missing_docs)]`, clippy `-D warnings` with the no-panic set (`unwrap_used`/`expect_used`/`panic`/`unreachable`/`indexing_slicing`/`cast_possible_truncation`), `fmt --check`, `cargo deny`, MSRV-1.64 job, `overflow-checks=true` in **all** profiles, doctests, the `usize`-in-serialized-fields grep gate, the `HashMap`-in-fit-path grep gate, the `unwrap`/`expect`/`#[allow(indexing_slicing)]`-needs-`// JUSTIFIED:` greps. **Flag any plan where feature code can merge before these exist** (§14 Phase 0 / Gate G0).
- The five Invariant checks must exist as `Result<(), PbError>` functions in Phase 0 over hand-built fixtures (Gate G0), *before* §08 lands — verify the plan front-loads `assert_exact_decomposition` skeleton.
- Strict phase order must hold: scaffold(§02/§13/§10-stubs) → binning(§03) → engine-on-SquaredError(§06←§05 SquaredError) → **G3 five-gate green spine**(§08) → loss set(§05) → interactions(§07) → serialize/score(§10) → Python(§12). **Flag** any §08 work before a green §06; any §07 interaction funnel before G3; any nonlinear loss before SquaredError reconstruction is gated; any quantized-hist/Fisher-TS/distill/ensemble (all v1.5) appearing inside v1. Determinism `{1,2,8}`-thread byte-equality must gate from Phase 2 (Gate G2), not appear at the end.
- **Flag** any task that depends on a task whose DoD has not yet turned a gate green ("builds on unverified foundation").

**(2) COMPLETENESS — every section 01–14 and every §13 gate maps to a task**
- Section coverage: 01(milestone/glossary) 02(layout/`PbError`/`Backend`/flag-matrix/`schema_version`) 03(binning/`BorderGrid` R-BINS/`Train`vs`ServeBinnedMatrix`/missing-bin/exposure) 04(Fisher-TS/`CatEncoderStore`/multi-cat-axis/audit-on-serve/combination-CTR-forbidden) 05(`Loss` trait/5 losses/`Metric`/`BlendedLoss`) 06(engine/`Hist`/Newton-gain/`QuantGradHess`/MVS/re-seeding) 07(`InteractionPolicy`/`MonotoneMap`/`wht8`/admission funnel) 08(`EffectTable`/`TableBank`/purify/5 checks/Sobol/SHAP) 09(distill/refit/Nesterov/ensemble/DART) 10(serde_json+bincode-2.x/`ScoringBank`/`PackedTree`/`RatingExport`/`ModelDoc`) 11(cache layout/SIMD permute/determinism harness) 12(PyO3/abi3-py310/sklearn/`.pyi`) 13(all gates) 14(roadmap).
- Every §13 gate must map to a named CI job/test: §13.1 five checks (each a separate `#[test]`, Purity is CHECK→GATE on §08 land, VarianceSum gated only under product/uniform `w`); 13.2 I1 feature-budget proptest; 13.3 proptest suites (binning round-trip, purification idempotent/mass-conserving/linear/perm-invariant, monotonicity, loss FD-check, loss-domain-error variants, serde round-trip); 13.4 thread-invariance + cross-run + the grep gates; 13.5 Newton-gain/quantization-bound/degenerate-input is_finite sweep; 13.6 cargo-fuzz `fuzz_deserialize`/`fuzz_binning`; 13.7 Criterion + xtask accuracy + TreeSHAP oracle; **13.7a the `wht8`↔purification cross-check oracle**; 13.8 lints/no-panic/unsafe/no-`Box<dyn Error>`/correct-variant-per-fn; 13.9 deny/MSRV/semver-checks; 13.10 CI matrix + Python CI + wheel-smoke + coverage; 13.12 exhaustive-vs-sampled-corners nightly.
- **Watch for these commonly-orphaned spec decisions:** the `ModelSchema` block + `schema_version` round-trip; `TrainBinnedMatrix` vs `ServeBinnedMatrix` and the audit-on-serve re-encode rule; the **table-size budget** (`max_table_cells` + total-bank budget + sparse-tensor fallback + `PbError::TableBudget`) and the **v1.5 table-budget stress benchmark**; the **`wht8` cross-check oracle** (§13.7a — easy to miss, it is a distinct test from the screening accumulator); the **reference-cell rating view** / `RatingExport`/`RatingTable`/`AxisExport` and the exposure-anchored base-level=1.000; the negative gate that `wht8` accumulator is never read by `assert_exact_decomposition`; the firewall `ExactnessMode` flip wired as both runtime + test; the frozen `bincode::config::standard()` byte-equality sub-check; the `splitmix64`/`Pcg64` re-seed determinism; `RefMeasure::Joint` VarianceSum→Shapley downgrade to CHECK.

**(3) DOES QUALITY OCCUR NATURALLY — structural, not bolted-on**
- DoD must be **"named gates green," never "write tests."** Flag any task whose DoD says "add tests"/"ensure correctness"/"test thoroughly" instead of citing specific gates (e.g. "G3 ThreeWayEqual green over the §13.1 matrix," "13.4 thread-invariance byte-equal," "13.8 correct-variant unit test").
- Invariant/test-first must be visible: the gate is authored alongside or before the feature in the same PR/milestone (e.g. the determinism harness exists at Phase 2, the five checks exist at Phase 0). Flag "audit later" framing.
- Illegal-states-unrepresentable: tasks should rely on construction-time I1 enforcement in `ObliviousTree::new`, the typed `ExactnessMode` firewall, and `Result<T,PbError>` everywhere — not on runtime asserts. Flag any plan reintroducing `Box<dyn Error>`, panics, or untyped failure.
- Traceability: every task must cite a spec §-ref and every §13 gate must name its CI job. Flag untraceable tasks.

**(4) REALISM — sizing/sequencing sane**
- Phase 2 (oblivious Newton histogram engine + subtraction trick + determinism) and Phase 3 (accumulate→purify→five gates) are the two genuinely large, risk-bearing tasks — flag if either is sized S/M or lumped into one PR. Small reviewable PRs on branches with trunk-always-green; flag mega-PRs.
- Criterion benches and proptest must be wired early (Phase 2), not deferred — flag if perf-regression visibility or property tests arrive only near release.

I'll produce the full prioritized, area-keyed findings list (~800–1200 words) as soon as the area plans are provided.
